# Интеграция отправки заказов в Telegram

## 📋 Описание

Реализована отправка данных из формы заказа в Telegram-чат через Telegram-бота без перезагрузки страницы.

## 📁 Файлы проекта

### 1. **send_order.php** (корень сайта)
   - Серверный скрипт для обработки заказов
   - Отправляет данные в Telegram через API бота
   - **Расположение:** корневая директория сайта (`/send_order.php`)

### 2. **parts/Оформить заказ/Checkout.html**
   - HTML-форма заказа с обновленными атрибутами `name`:
     - `name="name"` - ФИО
     - `name="phone"` - Телефон
     - `name="address"` - Адрес доставки
     - `name="message"` - Комментарий/список товаров

### 3. **parts/Оформить заказ/Js/checkout.js**
   - JavaScript-код для асинхронной отправки формы
   - Использует Fetch API
   - Обработка ответов и показ уведомлений

### 4. **parts/Оформить заказ/CSS/parts/Checkout.css**
   - Стили для уведомлений (успех/ошибка)

## 🔧 Настройка

### Данные для интеграции (уже внесены в код):
- **Токен Telegram-бота:** `8315054422:AAFhru9oaG6IANlHJIYEV-60qlrr6uiataQ`
- **ID чата Telegram:** `8031234667`

### Если нужно изменить:
Откройте файл `send_order.php` и измените значения:
```php
$botToken = 'ВАШ_ТОКЕН_БОТА';
$chatId = 'ВАШ_ID_ЧАТА';
```

## 📝 Формат сообщения в Telegram

При отправке заказа в Telegram придет сообщение в следующем формате:

```
🛒 НОВЫЙ ЗАКАЗ

👤 ФИО: [Имя клиента]
📱 Телефон: [Телефон]
📍 Адрес доставки: [Адрес]

📦 Товары в заказе:
━━━━━━━━━━━━━━━━━━━━
1. [Название товара]
   Количество: X шт.
   Размер: [Размер] (если есть)
   Цвет: [Цвет] (если есть)
   Цена: X ₽

━━━━━━━━━━━━━━━━━━━━
💰 Итого: X ₽

💬 Комментарий:
[Комментарий клиента]

🕐 Дата: DD.MM.YYYY HH:MM:SS
```

## ✅ Функционал

1. **Асинхронная отправка** - форма отправляется без перезагрузки страницы
2. **Валидация данных** - проверка обязательных полей перед отправкой
3. **Уведомления** - всплывающие уведомления об успехе/ошибке
4. **Список товаров** - автоматическое формирование списка товаров из корзины
5. **Очистка корзины** - автоматическая очистка после успешной отправки

## 🚀 Тестирование

1. Добавьте товары в корзину
2. Перейдите на страницу оформления заказа
3. Заполните форму:
   - ФИО (обязательно)
   - Телефон (обязательно)
   - Адрес доставки (обязательно)
   - Комментарий (необязательно)
4. Нажмите "Подтвердить заказ"
5. Проверьте уведомление и сообщение в Telegram

## ⚠️ Важно

- **PHP должен быть установлен** на сервере для работы скрипта
- **cURL должен быть включен** в PHP для отправки запросов к Telegram API
- **Файл send_order.php должен быть доступен** по пути `../../send_order.php` от страницы Checkout.html
- Проверьте права доступа к файлу (должен быть выполним)

## 🔒 Безопасность

- Все данные очищаются через `htmlspecialchars()` и `trim()`
- Валидация обязательных полей на клиенте и сервере
- Защита от XSS через экранирование HTML
- Использование POST-запросов для передачи данных

## 📞 Поддержка

При возникновении проблем проверьте:
1. Консоль браузера (F12) на наличие ошибок JavaScript
2. Логи сервера для ошибок PHP
3. Корректность токена бота и ID чата
4. Доступность Telegram API (может быть заблокирован в некоторых регионах)

